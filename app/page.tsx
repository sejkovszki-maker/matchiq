'use client';

import { useEffect, useMemo, useState } from 'react';
import { Activity, AlertTriangle, BarChart3, Brain, ChevronRight, Database, Flame, Goal, Heart, Home, Search, Settings, ShieldCheck, Star, Trophy, X } from 'lucide-react';
import { loadDailyData } from '@/lib/data';
import { analyzeMatch, analysisRank } from '@/lib/prediction/model';
import { percent } from '@/lib/prediction';
import { saveSnapshot } from '@/lib/data/snapshots';
import type { DataHealth } from '@/lib/data/providers/types';
import { ModelLab } from './model-lab';
import type { MarketKey, MatchAnalysis, MatchStatus } from '@/types/domain';

const nav=[[Home,'Mai meccsek'],[Flame,'Top jelzések'],[Trophy,'Ligák'],[Search,'Meccskereső'],[Brain,'Modell Labor'],[BarChart3,'Teljesítmény'],[Heart,'Kedvencek'],[Settings,'Beállítások']] as const;
const labels:Record<MarketKey,string>={home:'Hazai',draw:'Döntetlen',away:'Vendég',over15:'Over 1.5',over25:'Over 2.5',over35:'Over 3.5',bttsYes:'BTTS Igen'};
const statusLabels:Record<MatchStatus,string>={scheduled:'Tervezett',live:'Élő',finished:'Véget ért',postponed:'Elhalasztva',cancelled:'Törölve'};
const time=(iso:string)=>new Intl.DateTimeFormat('hu-HU',{hour:'2-digit',minute:'2-digit'}).format(new Date(iso));
const pct=(v:number|undefined)=>v===undefined?'—':`${percent(v)}%`;
const edge=(v:number|undefined)=>v===undefined?'—':`${v>=0?'+':''}${(v*100).toFixed(1)}%`;
const ev=(v:number|undefined)=>v===undefined?'—':`${v>=0?'+':''}${(v*100).toFixed(1)}%`;
const isoDate=(date:Date)=>`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`;
const fullDate=(date:Date)=>new Intl.DateTimeFormat('hu-HU',{year:'numeric',month:'long',day:'numeric',weekday:'long'}).format(date).toLocaleUpperCase('hu-HU');
const signalLabels={STRONG:'ERŐS JELZÉS',GOOD:'JÓ JELZÉS',WATCH:'FIGYELŐLISTA',NO_VALUE:'NINCS VALUE',AVOID:'KERÜLENDŐ'} as const;

function Confidence({value}:{value:number}){return <span className={`confidence ${value>=85?'hot':''}`}>{value>=85?<Flame size={14}/>:<ShieldCheck size={14}/>} {value}</span>}

function MatchCard({match,favorite,onFavorite,onOpen}:{match:MatchAnalysis;favorite:boolean;onFavorite:()=>void;onOpen:()=>void}){
  const main=match.model.bestSignal?match.model.markets.find(m=>m.market===match.model.bestSignal?.market)!:match.model.markets.filter(m=>['home','draw','away'].includes(m.market)).sort((a,b)=>b.modelProbability-a.modelProbability)[0];
  return <article className="match-card">
    <div className="match-top"><div><span className={`status-badge ${match.status}`}>{statusLabels[match.status]}</span><span className="time">{time(match.kickoff)}</span><span className="league">{match.league.name}</span></div><div className="top-actions">{match.isTippmix&&<span className="tag"><Star size={12} fill="currentColor"/>{match.source==='demo'?'Tippmix demo':'Tippmix adat'}</span>}<button className={`favorite ${favorite?'active':''}`} onClick={onFavorite} aria-label="Kedvenc"><Heart size={16} fill={favorite?'currentColor':'none'}/></button></div></div>
    <div className="teams"><strong>{match.home.name}</strong><span>–</span><strong>{match.away.name}</strong></div>
    <div className="prediction-row"><div><small>MODELL VÁRHATÓ EREDMÉNY</small><b>{match.model.predictedScore.join('–')}</b></div><div className="probabilities">{(['home','draw','away'] as MarketKey[]).map((k)=><span key={k}><i>{k==='home'?'1':k==='draw'?'X':'2'}</i><b>{pct(match.model.markets.find(m=>m.market===k)?.modelProbability)}</b></span>)}</div></div>
    <div className="signal-class">{main.signal?signalLabels[main.signal]:'NINCS PIACI ADAT'}</div><div className="value-strip"><span><small>{labels[main.market]}</small><b>{pct(main.modelProbability)}</b></span><span><small>Odds</small><b>{main.odds?.toFixed(2)??'—'}</b></span><span><small>Piac</small><b>{pct(main.marketProbability)}</b></span><span className={main.valueEdge!==undefined&&main.valueEdge>0?'positive':''}><small>Edge / EV</small><b>{edge(main.valueEdge)} · {ev(main.expectedValue)}</b></span></div>
    <div className="market-row"><Confidence value={match.model.confidence}/><span>DATA <b>{match.model.dataQuality}</b></span><span>STABIL <b>{match.model.predictionStability}</b></span></div>
    <button className="detail-btn" onClick={onOpen}>Modell vs Piac <ChevronRight size={16}/></button>
  </article>
}

function Detail({match,onClose}:{match:MatchAnalysis;onClose:()=>void}){
  const oneXtwo=match.model.markets.filter(m=>['home','draw','away'].includes(m.market));
  const advanced=match.model.advanced;
  const matrix=match.model.matrix.filter(c=>c.home<=4&&c.away<=4);
  const cell=(h:number,a:number)=>matrix.find(c=>c.home===h&&c.away===a)?.probability??0;
  return <div className="detail-overlay" role="dialog" aria-modal="true"><div className="detail-panel"><button className="close" onClick={onClose} aria-label="Bezárás"><X/></button><div className="detail-heading"><span>{match.league.name} · {time(match.kickoff)} · {statusLabels[match.status]}</span><h2>{match.home.name} <em>–</em> {match.away.name}</h2></div>
    <div className="hero-prediction"><div><small>ADVANCED TEAM STRENGTH ENGINE {match.model.version}</small><strong>{match.model.predictedScore.join('–')}</strong><p>Várható gól: <b>{advanced?.expectedGoals[0].toFixed(2)}</b> – <b>{advanced?.expectedGoals[1].toFixed(2)} xG</b></p></div><div className="hero-scores"><Confidence value={match.model.confidence}/><span>Adatminőség <b>{match.model.dataQuality}/100</b></span></div></div>
    <div className="detail-grid"><section className="panel wide"><h3>Modell vs Piac</h3><div className="compare-grid">{oneXtwo.map(m=><div className="compare" key={m.market}><div><b>{labels[m.market]}</b><span>Odds {m.odds?.toFixed(2)??'—'}</span></div><div className="dual-bar"><span className="model" style={{width:pct(m.modelProbability)}}/><span className="market" style={{width:pct(m.marketProbability)}}/></div><div className="compare-values"><span>Modell <b>{pct(m.modelProbability)}</b></span><span>Piac <b>{pct(m.marketProbability)}</b></span><strong className={m.valueEdge!==undefined&&m.valueEdge>0?'positive':''}>{edge(m.valueEdge)}</strong></div></div>)}</div><div className="legend"><span><i className="model"/>Modell</span><span><i className="market"/>Normalizált piac</span></div></section>
    {advanced&&<><section className="panel"><h3>Modell konszenzus</h3><div className="consensus-list">{advanced.votes.map(v=><p key={v.model}><span>{v.model}</span><b>{v.label}</b></p>)}</div><div className="consensus-total">Konszenzus <b>{advanced.consensusCount}/{advanced.votes.length}</b></div></section><section className="panel"><h3>Csapaterő és Elo</h3><div className="strength-grid"><p><span>{match.home.name}</span><b>{advanced.homeStrength.overall.toFixed(2)}</b><small>Elo {advanced.homeElo}</small></p><p><span>{match.away.name}</span><b>{advanced.awayStrength.overall.toFixed(2)}</b><small>Elo {advanced.awayElo}</small></p></div><div className="strength-details"><span>Hazai támadás <b>{advanced.homeStrength.home.attack.toFixed(2)}</b></span><span>Vendég védelem <b>{advanced.awayStrength.away.defence.toFixed(2)}</b></span></div></section><section className="panel wide"><h3>Prediction Breakdown</h3><div className="impact-list">{advanced.impacts.map(i=><div key={i.label}><span>{i.label}</span><div><i className={i.direction} style={{width:`${Math.min(100,Math.max(8,Math.abs(i.value)*4))}%`}}/></div><b className={i.direction}>{i.value>=0?'+':''}{i.value.toFixed(1)}%</b></div>)}</div></section></>}{advanced&&<section className="panel wide"><h3>Csapathelyzet és mérkőzéskörnyezet</h3><div className="squad-compare">{[[match.home.name,advanced.context.home,advanced.context.homeAdjustment],[match.away.name,advanced.context.away,advanced.context.awayAdjustment]].map(([name,team,adjustment])=>{const t=team as typeof advanced.context.home,a=adjustment as typeof advanced.context.homeAdjustment;return <article key={String(name)}><h4>{String(name)}</h4><div><span>Squad Strength <b>{t.squad.squadStrength}</b></span><span>Availability <b>{t.squad.availability}</b></span><span>Expected XI <b>{t.lineup.confidence}%</b></span><span>Rotation Risk <b>{t.rotationRisk}</b></span><span>Rest <b>{t.restDays} nap</b></span><span>Schedule Load <b>{t.congestion}</b></span><span>Formation Stability <b>{t.lineup.formationStability}</b></span><span>Context Stability <b>{t.contextStability}</b></span></div><p>Összes xG-korrekció <b className={a.total>=0?'positive':'negative'}>{a.total>=0?'+':''}{a.total.toFixed(2)}</b></p></article>})}</div><div className="adjustment-flow"><span>Base xG <b>{advanced.context.baseXg[0].toFixed(2)} – {advanced.context.baseXg[1].toFixed(2)}</b></span><i>→</i><span>Keret / pihenő / rotáció</span><i>→</i><span>Final xG <b>{advanced.context.finalXg[0].toFixed(2)} – {advanced.context.finalXg[1].toFixed(2)}</b></span></div></section>}{advanced&&<section className="panel wide"><h3>Confidence komponensek</h3><div className="confidence-components">{Object.entries(advanced.context.confidenceComponents).map(([key,value])=><div key={key}><span>{key.replace(/([A-Z])/g,' $1')}</span><div><i style={{width:`${value}%`}}/></div><b>{value}</b></div>)}</div><p className="context-note">A keret- és kezdőadatok demonstrációsak. Hiteles élő forrás nélkül a rendszer nem jelöl játékost megerősített hiányzóként.</p></section>}<section className="panel"><h3>Gólpiacok</h3><div className="market-list">{(['over15','over25','over35','bttsYes'] as MarketKey[]).map(k=>{const m=match.model.markets.find(x=>x.market===k)!;return <p key={k}>{labels[k]} <b>{pct(m.modelProbability)}</b></p>})}</div></section>
    <section className="panel"><h3>Adatforrás és frissesség</h3><div className="data-checks"><p><span>Fixture</span><b>{match.source}</b></p><p><span>Statisztika frissítve</span><b>{new Date(match.statistics.updatedAt).toLocaleTimeString('hu-HU',{hour:'2-digit',minute:'2-digit'})}</b></p><p><span>Minta</span><b>{match.statistics.sampleSize} meccs</b></p><p><span>Odds</span><b>{match.odds?'Elérhető':'Hiányzik'}</b></p><p><span>Odds frissítve</span><b>{match.odds?new Date(match.odds.timestamp).toLocaleTimeString('hu-HU',{hour:'2-digit',minute:'2-digit'}):'—'}</b></p><p><span>Szolgáltató</span><b>{match.odds?.provider??'—'}</b></p><p><span>Prediction stability</span><b>{match.model.predictionStability}/100</b></p></div></section>
    <section className="panel wide"><h3>Pontos eredménymátrix</h3><div className="matrix"><div/>{[0,1,2,3,4].map(a=><b key={a}>{match.away.name} {a}</b>)}{[0,1,2,3,4].flatMap(h=>[<b key={'h'+h}>{match.home.name} {h}</b>,...[0,1,2,3,4].map(a=>{const v=percent(cell(h,a));return <span className={v>=10?'peak':''} key={h+'-'+a}>{v}%</span>})])}</div></section>
    {match.dataWarnings.length>0&&<section className="panel wide warnings"><AlertTriangle/><div><h3>Adathiány és korlátozások</h3>{match.dataWarnings.map(w=><p key={w}>{w}</p>)}</div></section>}
    </div></div></div>
}

export default function Page(){
  const[showLab,setShowLab]=useState(false);const[analyses,setAnalyses]=useState<MatchAnalysis[]>([]);const[health,setHealth]=useState<DataHealth|null>(null);const[loading,setLoading]=useState(true);const[filter,setFilter]=useState('Összes');const[query,setQuery]=useState('');const[selected,setSelected]=useState<MatchAnalysis|null>(null);const[favorites,setFavorites]=useState<string[]>([]);const[showSearch,setShowSearch]=useState(false);
  const today=useMemo(()=>new Date(),[]);
  useEffect(()=>{try{setFavorites(JSON.parse(localStorage.getItem('matchiq-favorites')||'[]'))}catch{};loadDailyData(isoDate(today),'live').then(({matches,statistics,odds,health})=>{const list=matches.flatMap(m=>{const stat=statistics.find(s=>s.matchId===m.id);return stat?[analyzeMatch(m,stat,odds.find(o=>o.matchId===m.id))]:[]}).sort((a,b)=>(Number(b.isTippmix)-Number(a.isTippmix))||analysisRank(b)-analysisRank(a));setAnalyses(list);setHealth(health);list.filter(m=>m.status==='scheduled').forEach(m=>saveSnapshot({id:`${m.id}-${m.model.version}`,matchId:m.id,createdAt:m.model.calculatedAt,modelVersion:m.model.version,statistics:m.statistics,odds:m.odds,result:m.model}));}).finally(()=>setLoading(false))},[today]);
  const toggleFavorite=(id:string)=>setFavorites(f=>{const n=f.includes(id)?f.filter(x=>x!==id):[...f,id];localStorage.setItem('matchiq-favorites',JSON.stringify(n));return n});
  const filtered=useMemo(()=>analyses.filter(m=>{const tag=filter==='Összes'||filter==='Ma'||filter==='Tippmix'&&m.isTippmix||filter==='Magyar'&&m.league.country==='Magyarország'||filter==='Top liga'&&m.league.tier===1||filter==='Kedvencek'&&favorites.includes(m.id);const q=query.toLocaleLowerCase('hu-HU');return tag&&(!q||`${m.home.name} ${m.away.name} ${m.league.name}`.toLocaleLowerCase('hu-HU').includes(q))}),[analyses,filter,query,favorites]);
  const strongest=[...analyses].filter(m=>m.model.bestSignal).sort((a,b)=>analysisRank(b)-analysisRank(a)).slice(0,3);
  if(showLab)return <ModelLab onBack={()=>setShowLab(false)}/>;
  return <main className="app-shell"><aside className="sidebar"><div className="brand"><div><Activity/></div><span>Match<b>IQ</b></span><small>v0.7 BETA</small></div><nav>{nav.map(([Icon,label],i)=><button className={i===0?'active':''} key={label} onClick={()=>{if(label==='Kedvencek')setFilter('Kedvencek');if(label==='Modell Labor')setShowLab(true)}}><Icon size={19}/><span>{label}</span></button>)}</nav><div className="model-status"><span/><div><b>Production pipeline</b><small>Live → cache → demo fallback · 0.7.0</small></div></div></aside>
  <section className="content"><header className="topbar"><div className="mobile-brand"><Activity/><b>MatchIQ</b></div><div className="live"><span/> {analyses.filter(m=>m.status==='live').length} élő mérkőzés</div><button className="search" onClick={()=>setShowSearch(v=>!v)}><Search size={17}/> Meccs vagy csapat keresése...</button><div className="date-pill">{fullDate(today)}</div></header><div className="page">
  {health&&<div className={`data-health ${health.state}`}><div><Database/><span><b>{health.mode==='live'?'ÉLŐ ADATFORRÁS':health.mode==='offline'?'OFFLINE CACHE':'DEMO FALLBACK'}</b><small>Utolsó frissítés: {new Date(health.updatedAt).toLocaleTimeString('hu-HU',{hour:'2-digit',minute:'2-digit'})} · {health.state}</small></span></div><p>{health.mode==='live'?'Az adatok automatikusan frissülnek.':'A live provider nincs bekötve; az itt látható adatok nem élő Tippmix-adatok.'}</p></div>}
  {showSearch&&<div className="search-box"><Search size={18}/><input autoFocus placeholder="Csapat vagy liga..." value={query} onChange={e=>setQuery(e.target.value)}/><button onClick={()=>{setQuery('');setShowSearch(false)}}><X size={17}/></button></div>}
  <div className="title-row"><div><p className="eyebrow">LIVE PRODUCTION & TIPPMIX INTEGRATION · V0.7 BETA</p><h1>Mai mérkőzések</h1><p>Fixture → statisztika → modell → odds → value → snapshot → eredmény.</p></div><div className="accuracy"><Goal/><div><small>MODELLVERZIÓ</small><b>Prediction Engine 0.7.0</b></div></div></div>
  <div className="filters">{['Összes','Tippmix','Top liga','Magyar','Ma','Kedvencek'].map(x=><button className={filter===x?'active':''} onClick={()=>setFilter(x)} key={x}>{x}</button>)}</div>
  <section className="signals"><div className="section-heading"><div><Flame/><span><b>Érvényes modelljelzések</b><small>Minimum Confidence + Data + Edge + EV szabályokkal</small></span></div><p>Nem garantált tippek</p></div><div className="signal-grid">{strongest.length?strongest.map((m,i)=>{const best=m.model.markets.find(x=>x.market===m.model.bestSignal?.market)!;return <button onClick={()=>setSelected(m)} className="signal" key={m.id}><span className="rank">0{i+1}</span><div><b>{m.home.name} – {m.away.name}</b><small>{m.league.name} · {time(m.kickoff)} · {signalLabels[best.signal!]}</small></div><span className="pick">{labels[best.market]}</span><strong>{edge(best.valueEdge)}</strong><Confidence value={m.model.confidence}/><ChevronRight/></button>}):<div className="empty-state">Nincs elég erős modelljelzés a minimumszabályok alapján.</div>}</div></section>
  <div className="list-heading"><div><h2>{filter==='Kedvencek'?'Kedvenc mérkőzések':'Mérkőzések'}</h2><span>{filtered.length} mérkőzés</span></div><small>{loading?'Adatok betöltése…':'Tippmix-adatot csak hiteles mező alapján jelölünk'}</small></div>
  {loading?<div className="empty-state">A napi adatfolyam betöltése…</div>:filtered.length?<div className="match-grid">{filtered.map(m=><MatchCard key={m.id} match={m} favorite={favorites.includes(m.id)} onFavorite={()=>toggleFavorite(m.id)} onOpen={()=>setSelected(m)}/>)}</div>:<div className="empty-state">Nincs a feltételeknek megfelelő mérkőzés.</div>}
  </div><nav className="mobile-nav">{nav.slice(0,5).map(([Icon,label],i)=><button className={i===0?'active':''} key={label}><Icon/><span>{label.split(' ')[0]}</span></button>)}</nav></section>{selected&&<Detail match={selected} onClose={()=>setSelected(null)}/>}</main>
}
