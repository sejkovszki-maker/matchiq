import { availabilityScore } from './availability';import { playerImpactScore } from './player-impact';import { replacementQuality } from './replacement-quality';import type { Player } from '@/types/player';import type { SquadReport } from '@/types/injury';import type { SquadState } from '@/types/squad';export function calculateSquadState(teamId:string,players:Player[],reports:SquadReport[],source:string,updatedAt:string):SquadState{const impacts=players.map(p=>({playerId:p.id,score:playerImpactScore(p),replacementQuality:replacementQuality(p,players)}));const availability=availabilityScore(reports,impacts);const missingImpact=Math.round(100-availability);const squadStrength=Math.round(players.sort((a,b)=>playerImpactScore(b)-playerImpactScore(a)).slice(0,11).reduce((s,p)=>s+playerImpactScore(p),0)/11);return{teamId,players,impacts,reports,availability,missingImpact,squadStrength,stability:Math.max(40,Math.round((availability+squadStrength)/2)),source,updatedAt}}
