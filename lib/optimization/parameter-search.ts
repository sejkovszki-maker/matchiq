import type { ExperimentParameters } from '@/types/experiment';export function parameterGrid(){const result:ExperimentParameters[]=[];for(const formWeight of[.1,.15,.2,.25,.3])for(const eloWeight of[.15,.2,.25,.3])for(const xgWeight of[.3,.35,.4,.45]){const homeWeight=1-formWeight-eloWeight-xgWeight;if(homeWeight>=.05&&homeWeight<=.25)result.push({formWeight,eloWeight,xgWeight,homeWeight})}return result}
