import type { LeagueProfile } from '@/types/league-profile';import type { FormRating,TeamStrength } from '@/types/team-rating';
export function expectedGoals(home:TeamStrength,away:TeamStrength,homeForm:FormRating,awayForm:FormRating,league:LeagueProfile):[number,number]{const homeFormMod=Math.max(.82,Math.min(1.18,.82+homeForm.home*.22));const awayFormMod=Math.max(.82,Math.min(1.18,.82+awayForm.away*.22));const h=league.homeGoalsAvg*home.home.attack*away.away.defence*homeFormMod*league.homeAdvantage;const a=league.awayGoalsAvg*away.away.attack*home.home.defence*awayFormMod;return[Math.max(.2,Math.min(3.8,h)),Math.max(.2,Math.min(3.8,a))]}
