import type { PredictionChange } from '@/types/prediction-change';export function createPredictionChange(matchId:string,previousProbability:number,newProbability:number,reason:PredictionChange['reason'],source:string,updatedAt:string):PredictionChange{return{matchId,previousProbability,newProbability,change:newProbability-previousProbability,reason,source,updatedAt,important:Math.abs(newProbability-previousProbability)>=.025}}
