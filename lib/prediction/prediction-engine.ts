import { MODEL_CONFIG } from '@/config/prediction-model';import { getElo,getFormMatches,getSeasonInput } from '@/lib/data/team-model-data';import { getLeagueProfile } from '@/lib/leagues/league-profile';import { calculatePrediction } from '@/lib/prediction';import { consensus,probabilityOutcome } from './consensus';import { expectedGoals } from './expected-goals';import { eloOutcome,eloReliability } from '@/lib/ratings/elo';import { calculateForm } from '@/lib/ratings/form-rating';import { calculateTeamStrength } from '@/lib/ratings/team-strength';import { quoteFor } from '@/lib/value/value-engine';import type { AdvancedModelOutput,ConsensusVote,Outcome,PredictionImpact } from '@/types/model-output';import type { Match,Odds,Statistics } from '@/types/domain';
const outLabel=(o:Outcome)=>o==='home'?'HAZAI':o==='away'?'VENDÉG':'DÖNTETLEN';
export function runAdvancedEngine(match:Match,statistics:Statistics,odds:Odds|undefined,dataQuality:number){const league=getLeagueProfile(match.league.id);const homeForm=calculateForm(match.home.id,getFormMatches(match.home.id));const awayForm=calculateForm(match.away.id,getFormMatches(match.away.id));const avg=(league.homeGoalsAvg+league.awayGoalsAvg)/2;const homeStrength=calculateTeamStrength(getSeasonInput(match.home.id,avg),league.homeGoalsAvg,league.awayGoalsAvg,homeForm);const awayStrength=calculateTeamStrength(getSeasonInput(match.away.id,avg),league.homeGoalsAvg,league.awayGoalsAvg,awayForm);let [h,a]=expectedGoals(homeStrength,awayStrength,homeForm,awayForm,league);h=h*.72+statistics.homeXg*.28;a=a*.72+statistics.awayXg*.28;const poisson=calculatePrediction(h,a);const poissonVote=probabilityOutcome(poisson.homeWin,poisson.draw,poisson.awayWin);const strengthDelta=homeStrength.overall-awayStrength.overall;const strengthVote:Outcome=strengthDelta>.12?'home':strengthDelta<-.12?'away':'draw';const he=getElo(match.home.id),ae=getElo(match.away.id);const formEloVote=eloOutcome(he+(homeForm.home-.7)*110,ae+(awayForm.away-.7)*110);const marketHome=quoteFor(odds,'home')?.probability??0,marketDraw=quoteFor(odds,'draw')?.probability??0,marketAway=quoteFor(odds,'away')?.probability??0;const marketVote=probabilityOutcome(marketHome,marketDraw,marketAway);const votes:ConsensusVote[]=[{model:'Poisson',outcome:poissonVote,label:outLabel(poissonVote)},{model:'Team Strength',outcome:strengthVote,label:outLabel(strengthVote)},{model:'Elo / Forma',outcome:formEloVote,label:outLabel(formEloVote)},{model:'Piac',outcome:marketVote,label:outLabel(marketVote)}];const agreed=consensus(votes);const impacts:PredictionImpact[]=[{label:`${match.home.name} támadóereje`,value:(homeStrength.home.attack-1)*100,direction:homeStrength.home.attack>=1?'positive':'negative'},{label:`${match.away.name} idegenbeli védekezése`,value:(awayStrength.away.defence-1)*100,direction:awayStrength.away.defence>1?'positive':'negative'},{label:'Hazai pálya',value:(league.homeAdvantage-1)*100,direction:'positive'},{label:`${match.home.name} forma`,value:(homeForm.home-.7)*20,direction:homeForm.home>=.7?'positive':'negative'},{label:'Elo-különbség',value:(he-ae)/28,direction:he>=ae?'positive':'negative'}];const formStability=(homeForm.stability+awayForm.stability)/2;const eloRel=eloReliability(he,ae);const oddsStability=odds?.timestamp?.8:.35;const c=MODEL_CONFIG.confidence;const confidence=Math.round(100*(dataQuality/100*c.dataQuality+agreed.agreement*c.modelAgreement+formStability*c.formStability+eloRel*c.eloReliability+oddsStability*c.oddsStability+league.predictability*c.leaguePredictability));const advanced:AdvancedModelOutput={expectedGoals:[h,a],homeStrength,awayStrength,homeForm,awayForm,homeElo:he,awayElo:ae,votes,consensusOutcome:agreed.outcome,consensusCount:agreed.count,impacts,modelAgreement:agreed.agreement,formStability,eloReliability:eloRel,leaguePredictability:league.predictability};return{poisson,confidence:Math.max(35,Math.min(96,confidence)),advanced}}
