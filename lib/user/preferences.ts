import type { UserPreferences } from '@/types/user-preferences';
export const DEFAULT_PREFERENCES:UserPreferences={preferredLeagueIds:[],favoriteTeamIds:[],favoriteMatchIds:[],watchlistMatchIds:[],timezone:'Europe/Budapest',oddsFormat:'decimal',experienceMode:'simple',cardView:'detailed',defaultFilter:'Összes',notifications:{favoritesOnly:true,minimumGrade:'A',minimumValueScore:80,tippmixOnly:false}};
const KEY='matchiq-user-preferences-v09';export function loadPreferences(){if(typeof window==='undefined')return DEFAULT_PREFERENCES;try{return{...DEFAULT_PREFERENCES,...JSON.parse(localStorage.getItem(KEY)||'{}')} as UserPreferences}catch{return DEFAULT_PREFERENCES}}export function savePreferences(value:UserPreferences){if(typeof window!=='undefined')localStorage.setItem(KEY,JSON.stringify(value))}
