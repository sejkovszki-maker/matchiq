import type { HistoricalMatch } from '@/types/historical-match';import { bettingMetrics } from '@/lib/metrics/roi';export function simulateValueBets(rows:HistoricalMatch[],minEdge=.05,minConfidence=.7){const profits:number[]=[];for(const r of rows){if(!r.closingOdds)continue;const probs=[r.predictionSnapshot.home,r.predictionSnapshot.draw,r.predictionSnapshot.away];const market=r.closingOdds.map(o=>1/o);const total=market.reduce((a,b)=>a+b,0);const normalized=market.map(p=>p/total);const choice=probs.indexOf(Math.max(...probs));const edge=probs[choice]-normalized[choice];if(edge<minEdge||Math.max(...probs)<minConfidence)continue;const outcome=r.homeGoals>r.awayGoals?0:r.homeGoals===r.awayGoals?1:2;profits.push(choice===outcome?r.closingOdds[choice]-1:-1)}return{selections:profits.length,...bettingMetrics(profits)}}
