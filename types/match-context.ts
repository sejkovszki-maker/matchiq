import type { ExpectedLineup } from './lineup';import type { SquadState } from './squad';export type TeamContext={teamId:string;squad:SquadState;lineup:ExpectedLineup;restDays:number;matchesLast14Days:number;matchesLast30Days:number;minutesLast14Days:number;travelDistance:number;internationalTravel:boolean;timeZoneDifference:number;rotationRisk:number;congestion:number;manager:string;managerSince:string;matchesUnderManager:number;recentManagerChange:boolean;importance:number;contextStability:number};
export type ContextAdjustment={squad:number;rest:number;rotation:number;context:number;total:number};
export type MatchContextResult={home:TeamContext;away:TeamContext;homeAdjustment:ContextAdjustment;awayAdjustment:ContextAdjustment;baseXg:[number,number];finalXg:[number,number];advantage:number;confidenceComponents:{dataQuality:number;modelAgreement:number;formStability:number;lineupCertainty:number;squadStability:number;marketStability:number;contextStability:number};warnings:string[]};
