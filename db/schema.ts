export const historicalMatchesSchema=`CREATE TABLE IF NOT EXISTS historical_matches (match_id TEXT PRIMARY KEY,date TEXT NOT NULL,league_id TEXT NOT NULL,season TEXT NOT NULL,home_team_id TEXT NOT NULL,away_team_id TEXT NOT NULL,home_goals INTEGER NOT NULL,away_goals INTEGER NOT NULL,home_xg REAL NOT NULL,away_xg REAL NOT NULL,home_elo_before REAL NOT NULL,away_elo_before REAL NOT NULL,home_form_before REAL NOT NULL,away_form_before REAL NOT NULL,opening_odds_json TEXT,closing_odds_json TEXT,model_version TEXT NOT NULL,prediction_json TEXT NOT NULL,data_timestamp TEXT NOT NULL,prediction_timestamp TEXT NOT NULL,kickoff_timestamp TEXT NOT NULL,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`;
export const experimentsSchema=`CREATE TABLE IF NOT EXISTS model_experiments (id TEXT PRIMARY KEY,name TEXT NOT NULL,model_version TEXT NOT NULL,parameters_json TEXT NOT NULL,status TEXT NOT NULL,metrics_json TEXT,dataset_fingerprint TEXT,created_at TEXT NOT NULL)`;
