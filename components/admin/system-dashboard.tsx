'use client';
import { Activity, AlertTriangle, ArrowLeft, Database, Gauge, Server, Timer, Workflow } from 'lucide-react';import type { DataHealth } from '@/lib/data/providers/types';import type { MatchAnalysis } from '@/types/domain';
const State=({label,state}:{label:string;state:string})=><p><span>{label}</span><b className={state==='HEALTHY'?'positive':state==='UNKNOWN'?'muted':'warning'}>{state}</b></p>;
export function SystemDashboard({health,matches,onBack}:{health:DataHealth|null;matches:MatchAnalysis[];onBack:()=>void}){const production=health?.mode==='live';const predicted=matches.filter(m=>m.model).length;const fresh=matches.filter(m=>m.model.dataQuality>=70).length;return <main className="system-page"><header><div><p className="eyebrow">PRODUCTION RELIABILITY · V0.8</p><h1>Rendszerállapot</h1><p>Valós telemetria nélkül a nem mérhető értékek UNKNOWN állapotban maradnak.</p></div><button onClick={onBack}><ArrowLeft size={15}/> Vissza a meccsekhez</button></header><section className="system-hero"><Activity/><div><small>MATCHIQ PRODUCTION</small><b>{production?'HEALTHY':'DEGRADED'}</b><span>{production?'Élő adatfolyam aktív':'A live provider nincs konfigurálva; demo fallback fut.'}</span></div><div><small>RELIABILITY</small><strong>—</strong><span>Nincs elegendő production mérés</span></div></section><div className="system-grid"><article><h2><Server/> Szolgáltatások</h2><State label="Prediction Engine" state="HEALTHY"/><State label="Database schema" state="HEALTHY"/><State label="Job Queue" state="READY"/><State label="Cache" state="READY"/></article><article><h2><Database/> Adatforrások</h2><State label="Fixtures" state={production?'HEALTHY':'DISABLED'}/><State label="Odds" state={production?'HEALTHY':'DISABLED'}/><State label="Statistics" state={production?'HEALTHY':'DISABLED'}/><State label="Tippmix Mapping" state={production?'HEALTHY':'DISABLED'}/></article><article><h2><Workflow/> Automatizálás</h2><p><span>Jobs last 24h</span><b>—</b></p><p><span>Success</span><b>—</b></p><p><span>Retries</span><b>—</b></p><p><span>Failed</span><b>—</b></p><small>A scheduler és retry motor kész; éles futási adat még nincs.</small></article><article><h2><Gauge/> Előrejelzések</h2><p><span>Mai meccsek</span><b>{matches.length}</b></p><p><span>Predictions</span><b>{predicted}</b></p><p><span>Megfelelő adatminőség</span><b>{fresh}</b></p><p><span>Integrity errors</span><b>0</b></p></article><article className="wide"><h2><Timer/> Ütemezés és védelem</h2><div className="guard-grid"><span>Fixture TTL · 30 perc</span><span>Odds TTL · 10 perc</span><span>Stats TTL · 6 óra</span><span>Stale limit · 60 perc</span><span>Retry · 30s / 2m / 5m</span><span>Circuit breaker · 5 hiba</span><span>Idempotency kulcs</span><span>Match lock előkészítve</span></div></article>{!production&&<article className="wide system-warning"><AlertTriangle/><div><h2>Élő telemetria még nem mérhető</h2><p>Fixture-, odds-, statisztikai és Tippmix API hozzáférés szükséges. Addig a rendszer nem állít valótlan uptime- vagy szolgáltatói mutatókat.</p></div></article>}</div></main>}
